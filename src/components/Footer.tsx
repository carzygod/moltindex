const Footer = () => (
  <footer className="border-t border-slate-900 bg-slate-950/90 py-8">
    <div className="mx-auto max-w-6xl space-y-3 px-4 text-xs text-slate-500 sm:px-6">
      <p>Disclaimer: Moltindex is a curated directory maintained via local JSON data.</p>
      <p>Contact: agent@moltools.local</p>
      <p>Data source: locally maintained JSON files (not scraped from other sites).</p>
    </div>
  </footer>
);

export default Footer;
